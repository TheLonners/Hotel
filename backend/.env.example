PORT=3000
HOST=0.0.0.0
DATABASE_PATH=./data/hotel.sqlite
UPLOADS_DIR=./uploads
CORS_ORIGIN=*

# Opcional. Si lo defines, todas las llamadas /api requieren:
# Authorization: Bearer TU_PASSWORD
# o el header x-admin-password: TU_PASSWORD
ADMIN_PASSWORD=
